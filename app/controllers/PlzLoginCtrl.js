"use strict";

app.controller("PlzLoginCtrl", function($scope, $window, AuthFactory, $location, CardFactory){

// var loginPlz = $location.url("/loginplz");
            // $location.url("/loginplz");
            
            // var url = $location.url();
});